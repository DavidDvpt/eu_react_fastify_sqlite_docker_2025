version: "3.9"

services:
  sqlite:
    image: busybox
    volumes:
      - sqlite-dev:/data
    command:
      ["sh", "-c", "touch /data/dev.sqlite; while true; do sleep 3600; done"]

volumes:
  sqlite-dev:
