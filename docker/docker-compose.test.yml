services:
  db:
    image: postgres:16-alpine
    container_name: app_eu_db_test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app_dev
    ports:
      - "5434:5432"
    restart: unless-stopped
