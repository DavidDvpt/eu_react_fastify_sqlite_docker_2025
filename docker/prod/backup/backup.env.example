# Copy this file to .env on the server and fill the required values.
# The script reuses DATABASE_URL from the running API container, so no DB
# username or password is needed here.

API_CONTAINER_NAME=
BACKUP_DIR=/absolute/path/to/backups
RETENTION_DAYS=
BACKUP_PREFIX=
