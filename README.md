# TODO

## FRONT-END
- Creer un generic filter pour `manage`, reutilisable partout dans l'application.

## BACK-END
- A definir (pas de point note pour le moment).

## PRISMA/BDD
- Ajouter un versioning des mises a jour automatiques (table de version en base de donnees).

## AUTRE
- Creer un package `shared-type` pour mutualiser les types entre front et back.
